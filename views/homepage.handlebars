{{!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reel Talk</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <header>
        <h1>Movie Rating Homepage</h1>
    </header>
    <main> --}}
        <section class="hero">
            <h2>Welcome to Movie Ratings</h2>
            <p>Discover and rate your favorite movies!</p>
            <a href="#" class="btn">Explore Movies</a>
        </section>
        <section class="top-rated">
            <h2>Top Rated Movies</h2>
            <div class="movie-list">
                {{#each topRatedMovies}}
                    <div class="movie">
                        <h3>{{ title }}</h3>
                        <p>Rating: {{ rating }}/10</p>
                        <img src="{{ imageUrl }}" alt="{{ title }} Poster">
                        <a href="/movies/{{ id }}" class="btn">View Details</a>
                    </div>
                {{/each}}
            </div>
        </section>
    {{!-- </main>


</body>
</html> --}}
